// Canonical types live in shared/types.ts — re-export everything from there.
export {
  EVENTS,
  USER_STATUS,
  type EventValue,
  type UserStatus,
  type User,
  type Room,
  type Reaction,
  type Message,
  type JwtPayload,
} from '../../../shared/types';
